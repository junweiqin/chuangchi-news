# 创驰资讯站协作规则

## 站点定位

本仓库是创驰最新消息、事实纠偏、项目内容和询价归因的正式发布入口，线上域名为 `chuangchi.cc`，GitHub 仓库为 `junweiqin/chuangchi-news`。`njchuangchi.com` 是企业既有官网；两个站点角色不同，`chuangchi.cc` 不替代企业既有官网的工商主体核验口径。发布公司动态、公告、行业观点和重要进展前仍需获得明确发布指令。

## 新闻发布触发规则

- 用户明确要求“发布消息”“上线新闻”“更新官网”“同步到创驰网站”或同等含义时，默认任务范围包含：更新新闻数据、验证构建、提交 Git、推送 `main`，并检查 GitHub Pages 部署结果。
- 用户只要求起草、润色、研究、评审或制作待审稿时，不得自动对外发布。
- 发布任务完成后必须报告文章标题、发布日期、分类、线上状态和对应提交。

## 内容入口

- 当前新闻数据源是 `app/news-data.ts` 中的 `NEWS` 数组；每条消息生成独立的 `/news/{id}` 静态详情页。
- 新消息必须包含唯一 `id`、`category`、`date`、`title`、`summary` 和 `content`。
- `category` 只能使用：`公司动态`、`官方公告`、`创驰观察`。
- 最新重点消息可设置 `featured: true`，同时移除旧消息的 `featured`，用于 sitemap 优先级等重点内容信号。
- 默认按发布日期从新到旧排列；同日内容按实际发布时间或用户指定顺序排列。

## 事实口径

- 法定主体：南京创驰数字科技有限公司。
- 对外品牌简称：创驰广告；印刷业务品牌：创驰数字印刷；短称：创驰。
- 曾用法定名称：南京创驰广告有限公司。不得把曾用名写成当前法定主体；需要同时说明品牌和主体时，统一写作“南京创驰数字科技有限公司（对外品牌简称：创驰广告；印刷业务品牌：创驰数字印刷）”。
- 只发布用户确认或已有可靠证据支持的事实，不编造客户、案例、数据、资质、价格、合作或效果。
- 涉及品牌事实时优先查阅创驰 GEO 项目的事实库与来源台账；冲突内容必须先核验。

## GEO 执行主线

- 本项目所有工作以提升创驰在豆包、DeepSeek、腾讯元宝、千问、Kimi 等生成式搜索中的准确提及、推荐和引用为目标。
- 固定顺序为：可抓取与技术基础 → 主体和品牌统一 → 用户问题与业务答案 → 搜索引擎收录 → 地图及企业平台一致性 → 外部可信证据 → 固定问题集复测 → 按缺口迭代。
- 不以堆砌文章、关键词重复或虚构案例代替 GEO；没有测试结果支持时，不随意扩充内容。
- “印刷”属于全国性宽泛查询，阶段目标优先覆盖“南京印刷”“南京数字印刷”“南京企业画册印刷”“南京小批量印刷”等有地域和采购意图的查询，再逐步扩大。
- 对外提交、账号验证、平台资料修改和公开发布仍需用户确认；公开查询、代码检查、测试和执行记录维护可直接进行。
- 当前进度和完成条件以 `docs/geo-execution-plan.md` 为准。

## 发布检查

1. 检查标题、日期、分类、摘要、正文和头条状态。
2. 运行 `pnpm lint`、`pnpm test` 与 `GITHUB_PAGES=true pnpm exec next build`，并确保 GitHub Pages 静态构建兼容。
3. 检查提交中不含账号凭据、内部资料或未授权信息。
4. 提交并推送到 `main`，等待 `Deploy website` 工作流成功。
5. 验证 `chuangchi.cc` 返回最新标题；HTTPS 未就绪时同时说明证书状态。

保持改动聚焦于本次发布，不顺带重写历史消息或调整站点结构，除非用户明确要求。
